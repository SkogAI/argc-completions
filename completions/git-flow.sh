#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# {{ git-flow init
# @cmd Initialize a new git repo with support for the branching model.
# @flag -h --help                 Show this help
# @flag --showcommands            Show git commands while executing them
# @option -d <o]defaults>         Use default branch naming conventions
# @option -f <o]force>            Force setting of gitflow branches, even if already configured
# @option --no <o]force>          Force setting of gitflow branches, even if already configured
# @flag -p --feature              Feature branches
# @flag -b --bugfix               Bugfix branches
# @flag -r --release              Release branches
# @flag -x --hotfix               Hotfix branches
# @flag -s --support              Support branches
# @flag -t --tag                  Version tag prefix
# @flag --Use                     config file location
# @flag --no-Use                  config file location
# @flag --local                   use repository config file
# @flag --global                  use global config file
# @flag --system                  use system config file
# @flag --file*                   use given config file
# @flag --no-file*                use given config file
init() {
  :
}
# }} git-flow init

# {{ git-flow feature
# @cmd Manage your feature branches.
feature() {
  :
}
# }} git-flow feature

# {{ git-flow bugfix
# @cmd Manage your bugfix branches.
bugfix() {
  :
}
# }} git-flow bugfix

# {{ git-flow release
# @cmd Manage your release branches.
release() {
  :
}
# }} git-flow release

# {{ git-flow hotfix
# @cmd Manage your hotfix branches.
hotfix() {
  :
}
# }} git-flow hotfix

# {{ git-flow support
# @cmd Manage your support branches.
support() {
  :
}
# }} git-flow support

# {{ git-flow version
# @cmd Shows version information.
version() {
  :
}
# }} git-flow version

# {{ git-flow config
# @cmd Manage your git-flow configuration.
# @flag -h --help     Show this help
# @flag --Use         config file location
# @flag --no-Use      config file location
# @flag --local       Use repository config file
# @flag --global      Use global config file
# @flag --system      Use system config file
# @flag --file*       Use given config file
# @flag --no-file*    Use given config file
# @arg list
config() {
  :
}
# }} git-flow config

# {{ git-flow log
# @cmd Show log deviating from base branch.
log() {
  :
}
# }} git-flow log

command eval "$(argc --argc-eval "$0" "$@")"

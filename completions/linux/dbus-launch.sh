#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --auto-syntax                             you may explicitly specify a particular one by using
# @flag --sh-syntax                               for Bourne syntax, or --csh-syntax for csh syntax.
# @option --- <--aauuttoo--ssyynnttaaxx>  Choose --csh-syntax or --sh-syntax based on the SHELL environment variable.
# @option --- <--bbiinnaarryy--ssyynnttaaxx>  Write to stdout a nul-terminated bus address, then the bus PID as a binary integer of size sizeof(pid_t), then the bus X window ID as a binary integer of size sizeof(long).
# @option --- <--cclloossee--ssttddeerrrr>  Close the standard error output stream before starting the D-Bus daemon.
# @option --- <--ccoonnffiigg--ffiillee==FFIILLEENNAAMMEE>  Pass --config-file=FILENAME to the bus daemon, instead of passing it the --session argument.
# @option --- <--ccsshh--ssyynnttaaxx>  Emit csh compatible code to set up environment variables.
# @option --- <--eexxiitt--wwiitthh--xx1111>  If this option is provided, a persistent "babysitter" process will be created, and will connect to the X server.
# @option --- <--eexxiitt--wwiitthh--sseessssiioonn>  If this option is provided, a persistent "babysitter" process will be created, as if for --exit-with-x11.
# @option --- <--aauuttoollaauunncchh==MMAACCHHIINNEEIIDD>  This option implies that ddbbuuss--llaauunncchh should scan for a previously-started session and reuse the values found there.
# @option --- <--sshh--ssyynnttaaxx>    Emit Bourne-shell compatible code to set up environment variables.
# @option --- <--vveerrssiioonn>          Print the version of dbus-launch
# @option --- <--hheellpp>                   Print the help info of dbus-launch

command eval "$(argc --argc-eval "$0" "$@")"
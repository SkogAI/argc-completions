#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @option --R <RR>    Recursively list attributes of directories and their contents.
# @option --V <VV>    Display the program version.
# @option --a <aa>    List all files in directories, including files that start with `.'.
# @option --d <dd>    List directories like other files, rather than listing their contents.
# @option --l <ll>    Print the options using long names instead of single character abbreviations.
# @option --p <pp>    List the file's project number.
# @option --v <vv>    List the file's version/generation number.

command eval "$(argc --argc-eval "$0" "$@")"
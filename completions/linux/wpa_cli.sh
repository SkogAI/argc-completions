#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @meta inherit-flag-options
# @option --p <pp> <ppaatthh>          Change the path where control sockets should be found.
# @option --g <gg>                         ccoonnttrrooll ssoocckkeett ppaatthh Connect to the global control socket at the indicated path rather than an interface-specific control socket.
# @option --i <ii> <iiffnnaammee>    Specify the interface that is being configured.
# @option --h <hh>                         Help.
# @option --v <vv>                         Show version information.
# @option --B <BB>                         Run as a daemon in the background.
# @option --a <aa> <ffiillee>          Run in daemon mode executing the action file based on events from wpa_supplicant.
# @option --P <PP> <ffiillee>          Set the location of the PID file.
# @option --G <GG>                         ppiinngg iinntteerrvvaall Set the interval (in seconds) at which wpa_cli pings the supplicant.

command eval "$(argc --argc-eval "$0" "$@")"
#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @meta combine-shorts
# @option --R <RR>                            Recursively change attributes of directories and their contents.
# @option --V <VV>                            Be verbose with chattr's output and print the program version.
# @option --f <ff>                            Suppress most error messages.
# @option --v <vv> <_v_e_r_s_i_o_n>    Set the file's version/generation number.
# @option --p <pp> <_p_r_o_j_e_c_t>    Set the file's project number.

command eval "$(argc --argc-eval "$0" "$@")"